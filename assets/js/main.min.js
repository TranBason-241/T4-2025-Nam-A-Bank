$(document).ready(function () {
  var swiper1 = new Swiper(".swiper-1", {
    effect: "fade",
    navigation: {
      nextEl: ".swiper-container-1 .swiper-button-next",
      prevEl: ".swiper-container-1 .swiper-button-prev",
    },
    pagination: {
      el: ".swiper-container-1 .swiper-pagination",
    },
  });

  var swiper2 = new Swiper(".swiper-2", {
    effect: "fade",
    navigation: {
      nextEl: ".swiper-container-2 .swiper-button-next",
      prevEl: ".swiper-container-2 .swiper-button-prev",
    },
    pagination: {
      el: ".swiper-container-2 .swiper-pagination",
    },
  });

  //------------ NAV --------------

  navItems = document.querySelectorAll(".nav-item");
  navItems.forEach((navItem) => {
    navItem.addEventListener("click", () => {
      if (!navItem.classList.contains("active")) {
        //remove currentActive
        document.querySelector(".nav-item.active")?.classList.remove("active");
        navItem.classList.add("active");
        changePage(navItem);
      }
    });
  });

  pointerContainer = document.querySelector(".pointer-container");
  changePage = (navItem) => {
    className = navItem.getAttribute("value");
    targetPage = document.querySelector(`.${className}`);
    //remove current page
    document.querySelector(".page.active")?.classList.remove("active");
    targetPage.classList.add("active");
    swiper1.update();
    swiper2.update();
    window.scrollTo({
      top: 0,
      // behavior: 'smooth'
    });

    // reset aos
    $(".page.active [data-aos]").addClass("my-hide");
    $(".page.active [data-aos]").removeClass("aos-init").removeClass("aos-animate");
    $(".page.active [data-aos]").removeClass("my-hide");
    AOS.init();

    if (className == "page-0") {
      pointerContainer.classList.remove("show-back");
    } else {
      pointerContainer.classList.add("show-back");
    }
  };

  //Back to home

  pointerContainer.addEventListener("click", () => {
    document.querySelector(".nav-item.home").click();
  });

  const boxes = document.querySelectorAll(".fake-div");
  const tooltip = document.querySelector(".tool-tip");
  const parent = document.querySelector(".group_img_100");
  const popupContent = tooltip.querySelector(".popup-content");
  const tooltipText = tooltip.querySelector(".tooltip-text");
  let currentBox = null;

  // Mảng đối tượng chứa nội dung của tooltip và popup cho mỗi fake-div
  const contentData = [
    {
      tooltipText: "Xóa nghèo",
      popupContent: "Đóng góp 4,02 tỷ đồng cho các quỹ Vì người nghèo, hỗ trợ các gia đình khó khăn tại vùng kinh tế yếu và khu vực chịu thiên tai.",
    },
    {
      tooltipText: "Không còn nạn đói",
      popupContent: "Ưu tiên cho vay cho các dự án nông nghiệp xanh, sạch, tuần hoàn,… bằng các nguồn vốn ưu đãi với tổng dư nợ tại thời điểm 31/12/2024 là 584 tỷ đồng.",
    },
    {
      tooltipText: "Sức khỏe và có cuộc sống tốt",
      popupContent: ` <p>• 100% nhân viên được tham gia bảo hiểm y tế bắt buộc.</p>
                    <p>• Bổ sung quyền lợi về bảo hiểm sức khỏe cho một số đối tượng theo từng thời kỳ</p>
                    <p>• Khuyến khích nhân viên không sử dụng rượu bia trong giờ làm</p>
                    <p>• Phổ biến văn hóa trà đạo, tổ chức giải chạy và hội thao hàng năm cho tất cả CBNV. </p>
                    <p>• 100% nhân viên được tham gia bảo hiểm y tế bắt buộc.</p>
                    <p>• Tài trợ 4,07 tỷ đồng cho cơ sở y tế địa phương</p>`,
    },
    {
      tooltipText: "Giáo dục có chất lượng",
      popupContent: ` <p>• Đào tạo 240.132 giờ cho nhân viên trong năm.</p>
                    <p>• Tài trợ 2,84 tỷ đồng học bổng và cơ sở vật chất cho trường học vùng khó khăn.</p>`,
    },
    {
      tooltipText: "Bình đẳng giới",
      popupContent: ` <p>• Số lượng lao động nữ chiếm 55,6% tại Nam A Bank</p>
                    <p>• 3/6 thành viên HĐQT là nữ giới.</p>`,
    },
    {
      tooltipText: "Nước sạch và vệ sinh",
      popupContent: ` <p>•	Thường xuyên tổ chức hoạt động tiết kiệm, bảo vệ tài nguyên nước.</p>
      <p>• Nước thải từ các tòa nhà được kiểm định đạt chuẩn môi trường.</p>`,
    },
    {
      tooltipText: "Năng lượng sạch và giá thành hợp lý",
      popupContent: "Ưu tiên cho vay cho các dự án đầu tư xây dựng các nguồn năng lượng xanh và sạch bằng các nguồn vốn ưu đãi với tổng dư nợ tại thời điểm 31/12/2024 là 7.757 tỷ đồng.",
    },
    {
      tooltipText: "Công việc tốt và tăng trưởng kinh tế",
      popupContent: `
                    <p>• Lợi nhuận tăng gần 38% và tổng tài sản tăng gần 17% so với năm 2023.</p>
                    <p>• Duy trì mức tăng thu nhập bình quân trên mỗi nhân viên qua các năm.</p>
                    <p>• Ưu tiên giải ngân cho các dự án tạo thêm việc làm cho người dân địa phương tại các khu vực, địa bàn kinh tế chưa phát triển.</p>
                    `,
    },
    {
      tooltipText: "Công nghiệp, sáng tạo và phát triển hạ tầng",
      popupContent: "Đẩy mạnh số hóa, chuyển đổi số ngân hàng: mở 114 điểm giao dịch Onebank 365+ trên toàn hệ thống.",
    },
    {
      tooltipText: "Giảm bất bình đẳng",
      popupContent: "Lương khởi điểm tại các khu vực đồng đều, không có sự khác biệt về giới tính và vùng miền, cao hơn mức tối thiểu theo quy định của pháp luật hiện hành.",
    },
    {
      tooltipText: "Các thành phố và cộng đồng bền vững",
      popupContent: "Tài trợ xây dựng hạ tầng ở khu vực chưa phát triển.",
    },
    {
      tooltipText: "Tiêu thụ và sản xuất có trách nhiệm",
      popupContent: `
                    <p>• Tuân thủ chặt chẽ quy định môi trường – xã hội.</p>
                    <p>• Khuyến khích tiêu dùng xanh, tiết kiệm năng lượng.</p>
                    <p>• Đảm bảo an toàn dữ liệu khách hàng theo chuẩn quốc tế.</p>
                    `,
    },
    {
      tooltipText: "Hành động về khí hậu",
      popupContent: `
                    <p>• Xây dựng và thực thi khung quản trị rủi ro ESG xuyên suốt toàn bộ chuỗi giá trị.</p>
                    <p>• Huy động các nguồn vốn xanh để triển khai chương trình Tín dụng xanh.</p>
                    <p>• Khuyến khích và áp dụng các sáng kiến tiết kiệm năng lượng, giảm thiểu lượng phát thải khí nhà kính từ tiêu thụ nhiên liệu hóa thạch.</p>
                    `,
    },
    {
      tooltipText: "Tài nguyên và môi trường biển",
      popupContent: `
                    <p>• Tích cực ủng hộ và tham gia vào các dự án bảo vệ và phát triển rừng ven biển.</p>
                    <p>• Đánh giá tác động môi trường đối với các dự án sử dụng tài nguyên biển.</p>
                    `,
    },
    {
      tooltipText: "Tài nguyên và môi trường trên đất liền",
      popupContent: `
                    <p>• Trồng 5.000 cây xanh giai đoạn 2023–2024.</p>
                    <p>• Ưu tiên cho vay các dự án thân thiện môi trường, không ảnh hưởng hệ sinh thái, đa dạng sinh học của khu vực.</p>
                    `,
    },
    {
      tooltipText: "Hòa bình, công lý và các thể chế mạnh mẽ",
      popupContent: "Duy trì Quy chế dân chủ tại nơi làm việc, khuyến khích nhân viên phản ánh hành vi sai phạm.",
    },
    {
      tooltipText: "Quan hệ đối tác vì các mục tiêu",
      popupContent:
        "Hợp tác với Quỹ hợp tác khí hậu toàn cầu (Global Climate Partnership Funds – GCPF) và đã ký kết biên bản ghi nhớ hợp tác với Công ty quản lý Quỹ ResponsAbility (Thụy Sĩ) triển khai các chương trình Tín dụng xanh và quản lý rủi ro ESG trong hoạt động cấp tín dụng.",
    },
  ];

  let offsetPercent = 0;

  function positionTooltip(box) {
    const wasHidden = tooltip.style.display === "none" || getComputedStyle(tooltip).display === "none";

    if (wasHidden) {
      tooltip.style.visibility = "hidden";
      tooltip.style.display = "block";
    }

    const tooltipWidth = tooltip.offsetWidth || 500;
    const tooltipHeight = tooltip.offsetHeight || 200;

    if (wasHidden) {
      tooltip.style.display = "none";
      tooltip.style.visibility = "";
    }

    const boxRect = box.getBoundingClientRect();
    const parentRect = parent.getBoundingClientRect();
    const parentWidth = parent.offsetWidth;

    let top = boxRect.top - parentRect.top + boxRect.height / 2;
    let left = boxRect.left - parentRect.left + boxRect.width / 2;

    // Nếu có class "right" thì thêm offset
    const actualOffset = box.classList.contains("right") ? (offsetPercent / 100) * parentWidth : 0;
    left += actualOffset;

    // Lấy thông tin viewport
    const viewportWidth = window.innerWidth;
    const viewportHeight = window.innerHeight;

    // Lấy vị trí tooltip tính theo viewport (tạm thời)
    const tooltipX = boxRect.left + actualOffset + boxRect.width / 2;
    const tooltipY = boxRect.top + boxRect.height / 2;

    // Tính khoảng cách an toàn
    const safeMarginX = viewportWidth * 0.05; // 5% chiều rộng
    const safeMarginY = viewportHeight * 0.05; // 5% chiều cao

    // Giới hạn vị trí của tooltip để không vượt quá viewport (với margin 5%)
    const maxLeft = viewportWidth - tooltipWidth - safeMarginX;
    const minLeft = safeMarginX;

    const maxTop = viewportHeight - tooltipHeight - safeMarginY;
    const minTop = safeMarginY;

    // Áp dụng giới hạn
    let tooltipLeft = Math.min(Math.max(tooltipX - tooltipWidth / 2, minLeft), maxLeft);
    let tooltipTop = Math.min(Math.max(tooltipY - tooltipHeight / 2, minTop), maxTop);

    // Chuyển sang tọa độ tương đối với parent
    tooltip.style.left = `${tooltipLeft - parentRect.left}px`;
    tooltip.style.top = `${tooltipTop - parentRect.top}px`;
  }

  // Lắng nghe sự kiện hover và click trên các fake-div
  boxes.forEach((box, index) => {
    box.addEventListener("mouseenter", () => {
      const next = box.nextElementSibling;
      if (next) {
        next.classList.add("light");
      }

      if (tooltip.classList.contains("active")) return; // Nếu popup đang mở thì không hiển thị tooltip

      // Lấy dữ liệu từ mảng đối tượng `contentData`
      const { tooltipText: tooltipMsg } = contentData[index];

      tooltipText.innerHTML = tooltipMsg;
      popupContent.innerHTML = ""; // Clear nội dung popup khi chỉ hover
      positionTooltip(box);
      tooltip.style.display = "block";
    });

    box.addEventListener("mouseleave", () => {
      const next = box.nextElementSibling;
      if (!tooltip.classList.contains("active")) {
        tooltip.style.display = "none";
      }
      if (next) {
        next.classList.remove("light");
      }
    });

    box.addEventListener("click", (e) => {
      e.stopPropagation(); // Ngăn sự kiện click lan ra ngoài

      // Lấy dữ liệu từ mảng đối tượng `contentData` khi click
      const { tooltipText: tooltipMsg, popupContent: popupMsg } = contentData[index];
      tooltipText.innerHTML = tooltipMsg;
      popupContent.innerHTML = popupMsg; // Lấy nội dung popup từ mảng
      tooltip.classList.add("active");
      positionTooltip(box);
      tooltip.style.display = "block";
      currentBox = box;
    });
  });

  // Click bên ngoài để đóng popup
  document.addEventListener("click", (e) => {
    if (!tooltip.contains(e.target)) {
      tooltip.classList.remove("active");
      tooltip.style.display = "none";
      popupContent.innerHTML = "";
      currentBox = null;
    }
  });

  //btn close
  document.querySelector(".btn-close").addEventListener("click", () => {
    tooltip.classList.remove("active");
    tooltip.style.display = "none";
    popupContent.innerHTML = "";
    currentBox = null;
  });
});

// write function convert string to number

// write function change background when click on button has id = "haha"
